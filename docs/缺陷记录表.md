# 缺陷记录表

> 说明：本表分两部分——**A. 框架自身缺陷**（开发过程中发现并已修复，体现问题定位与修复能力）、
> **B. 被测系统缺陷**（通过横向对比探测发现，练习站刻意注入的缺陷）。
> 所有记录均来自真实过程，复现步骤、根因、验证方式可直接在仓库与 CI 记录中追溯。

## A. 框架 / 脚本自身缺陷

| 缺陷编号 | 模块 | 标题 | 严重级 | 优先级 | 状态 | 发现阶段 | 复现步骤 | 实际结果 | 预期结果 | 根因分析 | 修复方案与验证 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| BUG-FW-001 | Web-PO | `InventoryPage.go_to_cart` 方法不可用 | 高 | P1 | 已修复 | 代码评审 / 重构 | 1) 实例化 `InventoryPage` 2) 调用 `go_to_cart()` | 抛 `NameError`（`CartPage` 未导入）；且方法因缩进错误成为模块级函数 | 点击购物车入口并返回 `CartPage` 对象 | 方法缩进错误脱离类体；缺少 `from pages.cart_page import CartPage` | 方法收回类内并补导入；用例改为调用 PO 方法（`tests/test_login.py::test_add_to_cart_then_remove` 验证通过） |
| BUG-FW-002 | 公共-日志 | 日志写入"已关闭的流"报 ValueError | 中 | P2 | 已修复 | 本地运行 / CI 日志 | 1) 运行 pytest 全量 2) 观察 teardown 阶段输出 | 出现 `ValueError: I/O operation on closed file` 日志错误 | 日志应正常输出，不影响用例结果 | `StreamHandler(sys.stdout)` 在 import 时绑定原始流，pytest 捕获机制在 teardown 关闭 / 替换该流 | 自实现 `_ConsoleHandler`，每次 emit 动态获取当前 `sys.stdout`；文件输出不可写时降级为仅控制台（`common/logger.py`） |
| BUG-FW-003 | 测试框架 | 重复 conftest 导致 hook 执行两次、fixture 互相遮蔽 | 中 | P2 | 已修复 | 代码评审 | 1) 根目录与 `tests/` 各保留一份 conftest 2) 触发一次用例失败 | 失败截图逻辑执行两次；`driver` fixture 定义冲突 | 全局 fixture 与 hook 应单一来源 | 两处 conftest 同时注册了 `driver` 与 `pytest_runtest_makereport` | 收敛为唯一 `tests/conftest.py`，全部 fixture / hook 单一定义 |
| BUG-FW-004 | Web-稳定性 | CI（Chrome 无头）下点击 / 输入丢失导致用例失败 | 高 | P1 | 已修复 | CI 回归 | 1) 本地 Edge 运行 Web 用例（通过）2) 推送触发 CI（Chrome 无头）运行同一套用例 | 4 条购物车 / 结算用例失败：点击后页面无变化（无报错）；部分输入框 value 为空 | 与应用户操作等价：点击应生效并跳转 / 输入应被记录 | 被测站为 React 应用：① 点击落在重渲染替换前的旧节点；② 受控输入 value 异步提交 / 水合重置；③ 个别浏览器表单错误提示不渲染 | ① `click_until`：点击后校验状态提交（URL / 元素增删），Selenium 点击 ↔ JS `element.click()` 交替重试；② `input_text`：输入后轮询校验 value，未生效用 JS 原生 setter + input/change 事件兜底；③ 失败附加浏览器 Console 日志；④ 负向断言改为业务规则 + 文案可选校验。**验证：当时 CI 四个 Job 全绿、23 条用例通过；后续用例已扩充至 53 条，本地无头模式 53/53 全绿（CI 待复验）** |

## B. 被测系统缺陷（SauceDemo `problem_user`）

> 探测脚本：`scripts/defect_probe.py`（真实浏览器横向对比 standard_user / problem_user，2026-09-06 执行）

| 缺陷编号 | 模块 | 标题 | 严重级 | 优先级 | 状态 | 发现阶段 | 复现步骤 | 实际结果 | 预期结果 | 根因分析 |
|---|---|---|---|---|---|---|---|---|---|---|
| BUG-SUT-001 | 商品列表-展示 | `problem_user` 商品图片全部错乱 | 中 | P2 | 待官方修复（练习站刻意注入） | 缺陷探测 | 1) 用 `problem_user` / `secret_sauce` 登录 2) 打开商品列表页 | 6 件商品的 `<img>` 全部指向同一张 404 占位图（去重后仅 1 张） | 每件商品展示各自商品图（standard_user 为 6 张不同图片） | 前端图片映射逻辑异常（被站点刻意注入的缺陷） |
| BUG-SUT-002 | 商品列表-排序 | `problem_user` 商品排序功能失效 | 中 | P2 | 待官方修复（练习站刻意注入） | 缺陷探测 | 1) `problem_user` 登录 2) 选择 "Name (Z to A)" 3) 选择 "Price (low to high)" | 名称降序后首项不变；价格序列 `[29.99, 9.99, 15.99, 49.99, 7.99, ...]` 非升序 | 与 standard_user 一致：名称降序首项变为 "Test.allTheThings() T-Shirt (Red)"；价格为升序 | 排序逻辑对 problem_user 未生效（站点刻意注入的缺陷） |

**处理立场**：已知的被测系统缺陷**不写入回归套件断言**（否则回归永久为红、失去信号价值），
而是以「探测脚本 + 缺陷记录」方式留存，便于缺陷回归时人工 / 半自动复核。

## C. 缺陷统计

| 分类 | 数量 | 已修复 | 说明 |
|------|------|--------|------|
| 框架/脚本自身缺陷 | 4 | 4 | 全部修复并回归验证 |
| 被测系统缺陷 | 2 | — | 练习站刻意注入，已记录并留存复现脚本 |
| **合计** | **6** | **4** | 详见上表 |
