# 测试计划（项目版）

| 项目 | 内容 |
|------|------|
| 项目名称 | 全栈自动化测试框架（接口 + Web） |
| 被测试对象 | Petstore 接口（https://petstore.swagger.io/v2）、SauceDemo 电商网站（https://www.saucedemo.com/） |
| 计划版本 | v1.0 |
| 编写 / 执行 | 【侯静然】 |
| 日期 | 2026-09 |

---

## 1. 测试目标

1. 验证 Petstore 宠物管理接口（增删改查、状态流转、异常场景）行为与契约符合预期；
2. 验证 SauceDemo 核心用户路径（登录、购物车、商品排序、下单全流程、表单校验）；
3. 沉淀可配置、可重复执行、可观测、可接入 CI 的自动化回归资产。

## 2. 测试范围

### 2.1 纳入范围（In Scope）

| 模块 | 被测试功能 | 用例数 |
|------|------------|--------|
| 接口-宠物管理（CRUD） | 创建、按 ID 查询、更新、删除、删除后再查询 | 5 |
| 接口-异常场景 | 查询/删除不存在宠物、非法 status、缺必填字段、非法状态查询 | 5 |
| 接口-状态与数据驱动 | findByStatus 三取值过滤、JSON 数据驱动创建 3 组 | 6 |
| 接口-边界与契约 | 中文/符号名称、超长名称、重复 ID 覆盖、PUT upsert 契约、响应头与耗时 | 4 |
| Web-登录与登录态 | 正向 2、反向 4、退出登录 1 | 7 |
| Web-访问控制 | 未登录访问 inventory / cart 被重定向 | 2 |
| Web-商品列表与详情 | 排序 4 种、详情一致性、多商品加购、列表页移除 | 7 |
| Web-购物车 | 空态、继续购物、部分移除、移除最后一件、加购移除链路 | 5 |
| Web-下单 | 全流程 E2E、金额计算、取消下单、表单必填 3 场景 | 6 |
| **合计** | | **47** |

> 用例级明细见 [`测试用例表.md`](测试用例表.md)。

### 2.2 不纳入范围（Out of Scope）

- 性能 / 压力测试、安全测试、兼容性全矩阵（仅覆盖 Edge 与 Chrome 无头）；
- 被测对象为公开练习平台，不做业务需求级验收，仅作自动化框架能力演示；
- 移动端与其他浏览器不在本轮 CI 覆盖范围内。

## 3. 测试策略

| 维度 | 策略 |
|------|------|
| 分层设计 | 四层：用例层 → 页面 / 接口对象层 → 公共封装层 → 配置层；Web 采用 Page Object |
| 用例设计 | 正向 + 反向分离；参数化 + JSON 数据驱动；覆盖等价类（status 三取值）与异常输入（404、表单缺失） |
| 用例组织 | marker 分层：`api` / `web` / `smoke`（核心主流程 5 条）/ `negative`（反向 14 条） |
| 接口断言 | 三层：HTTP 状态码 + 业务字段 + jsonschema 响应结构约束 |
| 数据管理 | fixture 前置创建、`finally` 幂等清理、ID 随机化 → 用例可重复执行、环境无残留 |
| 稳定性 | 显式等待为主、等待时长可配置；失败重试仅限基础设施类异常（浏览器 / 驱动 / 网络），断言失败不重试 |
| 可观测性 | 失败自动收集：运行日志尾部 + 页面截图 + DOM 源码 + 浏览器 Console 日志（Allure 附件） |
| 配置管理 | `config.ini` 集中配置 + 环境变量覆盖（`TEST_WEB_HEADLESS` 等），本地与 CI 复用同一套用例 |
| 执行方式 | 单进程或 `pytest-xdist` 并行；CI 由 GitHub Actions 编排（push / 每日定时 / 手动触发） |

## 4. 测试环境

| 项 | 本地 | CI |
|----|------|----|
| 操作系统 | Windows 11 | ubuntu-latest |
| Python | 3.10+（3.14 实测） | 3.12 |
| 浏览器 | Edge（可切换无头） | Chrome 无头（Selenium Manager 自动匹配驱动） |
| 关键依赖 | pytest 9.1.1 / requests 2.34.2 / selenium 4.47.0 / allure-pytest 2.16.0 / jsonschema 4.26.0 / pytest-xdist 3.8.0 / pytest-rerunfailures 16.6.1 | 同左（`requirements.txt` 锁定版本） |
| 报告 | Allure CLI 2.45.0 本地生成 | Artifact + GitHub Pages 在线发布 |

## 5. 准入 / 准出准则

- **准入**：代码已提交且通过语法检查；依赖安装完成；被测公开环境可访问（HTTP 200）；
- **准出**：计划内 47 条用例 100% 执行完成；无未澄清的失败用例；Allure 报告产出且可访问；
  CI 流水线（接口 / Web / 报告 / 发布）全绿。

## 6. 进度安排（实际执行节奏）

| 阶段 | 内容 | 产出 |
|------|------|------|
| 第 1 阶段 | 框架骨架：配置、日志、请求封装、PO 基类 | 可运行的最小框架 |
| 第 2 阶段 | 接口用例：CRUD + 异常 + 数据驱动 + schema 校验 | 接口用例 12 条 |
| 第 3 阶段 | Web 用例：登录正反向 + 购物车 + 排序 + 下单 E2E | Web 用例 11 条 |
| 第 4 阶段 | 稳定性加固：跨浏览器（CI Chrome 无头）竞态处理 | CI 全绿 |
| 第 5 阶段 | 工程化收尾：Allure、失败取证、CI/CD、Pages 在线报告 | 流水线 + 在线报告 |
| 第 6 阶段 | 文档沉淀：测试设计 / 计划 / 用例表 / 缺陷记录 / 报告 | 本文档体系 |
| 第 7 阶段 | 体量扩充：接口健壮性/契约 8 条 + Web 鉴权/详情/金额/购物车/表单 16 条 | 用例 23 → 47 条 |

## 7. 风险与应对

| 风险 | 影响 | 应对措施 | 实际结果 |
|------|------|----------|----------|
| 公开环境网络波动（连接重置 / 超时） | 用例误报 | 重试仅针对基础设施类异常 + 失败取证 | 已生效（曾出现整批 `ERR_CONNECTION_*`，重跑即恢复） |
| React 应用在 CI 下点击 / 输入竞态 | 本地绿、CI 挂 | 点击 / 输入后校验状态提交，未达成交替用 JS 方式重试 | 已收敛为 CI 全绿 |
| 浏览器渲染差异（表单错误提示不渲染） | 负向断言误报 | 断言改为业务规则（校验拦截 = URL 未跳转）+ 文案可选校验 | 已解决 |
| 前端元素变更 | 用例失效 | 定位器集中在 PO；失败自动留 DOM 源码定位 | 机制就绪 |
| CI 首次下载浏览器驱动慢 | 流水线耗时增加 | Selenium Manager 自动匹配；必要时 Re-run | 已接受（首次 3~5 分钟） |

## 8. 交付物清单

1. 自动化用例资产（47 条）与 Allure 报告；
2. CI 流水线与在线报告（GitHub Actions + GitHub Pages）；
3. 文档体系：测试计划（本文）、测试用例表、测试设计说明、缺陷记录表、测试报告、缺陷与踩坑复盘。

## 9. 变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09 | 首版：按项目真实情况编写（范围、策略、环境、风险均来自实际执行结果） |
