# 测试报告（本项目最新执行结果）

| 项 | 内容 |
|----|------|
| 报告名称 | 全栈自动化测试框架（接口 + Web）回归测试报告 |
| 被测对象 | Petstore 接口、SauceDemo Web |
| 执行方式 | 本地全量回归（顺序 / 并行）+ GitHub Actions 流水线（接口 / Web 双 Job） |
| 执行环境 | 本地：Windows 11 + Python 3.14 + Edge；CI：ubuntu-latest + Python 3.12 + Chrome 无头 |
| 执行日期 | 2026-09 |
| 报告结论 | **通过**：47 条用例全部通过（含 1 次基础设施类重试后自愈），CI 流水线此前已全绿，本轮扩充后待流水线复验 |

---

## 1. 执行概况

| 指标 | 本地全量（并行 -n 4） | 本地无头（近似 CI） | 本地 Web 子集 | CI（GitHub Actions） |
|------|----------------------|--------------------|---------------|----------------------|
| 用例总数 | 47 | 47 | 27 | 47（待本轮复验；上一轮 23 条全绿） |
| 通过 | 47 | 47 | 27 | — |
| 失败 / 中断 / 跳过 | 0 / 0 / 0 | 0 / 0 / 0 | 0 / 0 / 0 | — |
| 通过率 | 100% | 100% | 100% | — |
| 耗时 | 62.14s（含 1 次重试） | 191.12s | 176.29s（顺序） | 待记录 |
| 报告 | Allure（本地 `allure open allure-report`） | 同左 | 同左 | Artifact + GitHub Pages 在线报告 |

> 说明：本轮为用例体量扩充（23 → 47 条），本地已完成两种模式全量验证
> （并行 47/47、无头 47/47，后者运行条件与 CI 的 Chrome 无头最接近）；
> CI 结果以推送后的流水线为准（本轮推送时 GitHub 网络不可达，待网络恢复后复验）。

## 2. 分模块结果

| 模块 | 用例数 | 通过 | 失败 | 备注 |
|------|--------|------|------|------|
| 接口-宠物管理（CRUD） | 5 | 5 | 0 | 含 jsonschema 结构校验 |
| 接口-异常场景 | 5 | 5 | 0 | 404 / 非法 status / 缺字段 / 非法状态查询 |
| 接口-状态与数据驱动 | 6 | 6 | 0 | 三取值参数化 + JSON 数据驱动 3 组 |
| 接口-边界与契约 | 4 | 4 | 0 | 中文/符号名称、超长名称、重复 ID 覆盖、PUT upsert 契约、响应头与耗时 |
| Web-登录与登录态 | 7 | 7 | 0 | 正向 2 + 反向 4 + 退出登录 |
| Web-访问控制 | 2 | 2 | 0 | 未登录访问 inventory / cart 重定向 |
| Web-商品列表与详情 | 7 | 7 | 0 | 排序 4 + 详情一致性 + 多商品加购 + 列表移除 |
| Web-购物车 | 5 | 5 | 0 | 空态、继续购物、部分移除、移除最后一件、加购移除链路 |
| Web-下单 | 6 | 6 | 0 | E2E、金额计算、取消下单、表单必填 3 场景 |
| **合计** | **47** | **47** | **0** | — |

## 3. 覆盖情况

| 覆盖维度 | 覆盖情况 |
|----------|----------|
| 接口方法覆盖 | GET / POST / PUT / DELETE 全覆盖 |
| 接口状态码覆盖 | 200（成功）、404（资源不存在）、400/200（非法入参容错） |
| 等价类覆盖 | `status` 三取值全覆盖；非法取值单独覆盖 |
| 边界数据覆盖 | 中文 + 表情符号、256 字符超长名称、随机大 ID |
| 幂等/覆盖语义 | 同 ID 重复创建以最后一次为准；PUT 不存在 ID 行为自洽 |
| 接口契约覆盖 | Content-Type、响应耗时、jsonschema 结构 |
| Web 用户路径 | 登录（正/反）→ 列表（排序/详情/加购/移除）→ 购物车（空态/继续/部分移除）→ 结算（金额/取消/校验）→ 下单完成 → 退出登录 |
| 安全/访问控制 | 未登录直接访问受保护页面被重定向（2 条） |
| 业务规则 | 金额计算（小计 + 税费 = 总计）、购物车数量一致性、表单必填校验 |

## 4. 缺陷情况

| 分类 | 数量 | 说明 |
|------|------|------|
| 框架/脚本自身缺陷 | 4（均已修复） | 详见 [`缺陷记录表.md`](缺陷记录表.md) |
| 被测系统缺陷 | 2（已记录，练习站刻意注入） | `problem_user` 图片错乱、排序失效 |
| 阻塞性缺陷 | 0 | 无 |

## 5. 稳定性与效率

| 项 | 结果 |
|----|------|
| 失败重试策略 | 仅对基础设施类异常（浏览器/驱动/网络）重试 1 次；断言失败不重试；本轮 47 条中触发 1 次重试并自愈 |
| 失败可观测 | 自动附加：运行日志尾部 + 页面截图 + DOM 源码 + 浏览器 Console 日志 |
| 跨环境一致性 | 同一套用例在本地 Edge（有头/无头）下均通过（47/47）；CI Chrome 无头此前已验证机制有效（环境变量切换浏览器 / 无头） |
| 并行执行 | `pytest -n 4` 全量 62.14s；单进程无头全量 191.12s |
| 重复执行 | 数据前置创建 + 幂等清理；47 条用例反复执行无残留 |

## 6. 风险与遗留问题

| 项 | 说明 | 处理 |
|----|------|------|
| 公开环境波动 | 可能整批 `ERR_CONNECTION_*` 导致误报 | 已由"仅基础设施类重试"覆盖，重跑即恢复 |
| 浏览器渲染差异 | 个别浏览器/无头下表单错误提示不渲染 | 负向断言采用"业务规则 + 文案可选校验" |
| 覆盖范围 | 未覆盖性能基线（仅 1 条响应耗时契约）、安全专项、多浏览器矩阵 | 属范围外，可作为后续扩展 |
| CI 时长 | 用例扩充后 Web Job 时间增长（约 3~6 分钟） | 已支持 `-n` 并行；必要时在 CI 启用 |

## 7. 结论与建议

- **结论**：47 条自动化用例全部通过，覆盖接口契约/边界/幂等语义与 Web 端到端业务规则
  （含访问控制与金额计算），可作为持续回归基线；流水线具备"提交即回归 + 报告自动发布"能力。
- **建议**：
  1. CI 中可开启 `pytest-xdist` 并行以控制时长；
  2. 关注 `problem_user` 两个已知缺陷是否被官方修复，可转为回归用例；
  3. 视需要补充性能基线与多浏览器矩阵。

## 8. 附录

- 在线报告：https://Oxob-hue.github.io/auto-test-framework/
- 代码仓库：https://github.com/Oxob-hue/auto-test-framework
- 相关文档：测试计划、测试用例表、测试设计说明、缺陷记录表、缺陷与踩坑复盘
