# 自动化测试项目依赖（Python 3.10+，已在 3.14 下验证通过）
# 安装：pip install -r requirements.txt
pytest==9.1.1
pytest-rerunfailures==16.6.1   # 失败自动重试（仅基础设施类失败）
pytest-xdist==3.8.0            # 并行执行（实测 -n 4 提速约 2.4 倍）
requests==2.34.2
selenium==4.47.0
allure-pytest==2.16.0
jsonschema==4.26.0             # 接口响应结构校验
